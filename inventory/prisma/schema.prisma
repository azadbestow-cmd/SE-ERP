datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

model Product {
  id    Int     @id @default(autoincrement())
  name  String
  stock Int
}